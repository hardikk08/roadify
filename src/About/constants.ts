const NS = '@app/About';

export const REDUX = `${NS}/REDUX`;
export const REDUX_SAGA = `${NS}/REDUX_SAGA`;
export const REDUX_LOGGER = `${NS}/REDUX_LOGGER`;
export const EMOTION = `${NS}/EMOTION`;

export const TEST_SAGA = `${NS}/TEST_SAGA`